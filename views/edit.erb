<!DOCTYPE html>
<h2> What do you wish to change "<%= @message.message %>" to? </h2>

<form action="/message/<%= @message.id %>" method="post">
  <textarea name="message[message]" id="edited_content" rows="10" cols="40"><%= @message.message %></textarea>
  <input type="submit" value="Submit">
</form>
